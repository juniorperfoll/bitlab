# Agent-local settings/credentials — never commit
.claude/settings.local.json

# Backend (Node.js monolith) — tooling and local dev state
backend/node_modules/
backend/dist/
backend/data/*.db
backend/data/*.db-*
backend/*.log
.env
.env.*

# Pacote de arte original (não redistribuir o .zip em si — só os recortes
# aplicados em backend/public/assets/moderninteriors/, permitido pela licença)
Modern_Interiors_Free_v2.2.zip

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
